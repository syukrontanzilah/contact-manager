import React from 'react'
import { StyleSheet, View } from 'react-native'

const Gaf = ({height, width}) => {
    return (
        <View style={{height:height, width:width}}>
        </View>
    )
}

export default Gaf

const styles = StyleSheet.create({})
